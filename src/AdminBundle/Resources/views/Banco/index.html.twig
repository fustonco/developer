{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Banco list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Codigo</th>
                <th>Nome</th>
                <th>Ativo</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('banco_show', { 'id': entity.id }) }}">{{ entity.codigo }}</a></td>
                <td>{{ entity.nome }}</td>
                <td>{{ entity.ativo }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('banco_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('banco_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('banco_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
