{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cartão</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Titulo</th>
                <td>{{ entity.titulo }}</td>
            </tr>
            <tr>
                <th>Número</th>
                <td>{{ entity.numero }}</td>
            </tr>
            <tr>
                <th>Bandeira</th>
                <td>{{ entity.bandeira }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cartoes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cartao_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
