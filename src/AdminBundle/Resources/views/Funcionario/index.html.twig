{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Funcionario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Email</th>
                <th>Senha</th>
                <th>Limiteaprovacao</th>
                <th>Telefone</th>
                <th>Celular</th>
                <th>Ativo</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('funcionario_show', { 'id': entity.id }) }}">{{ entity.nome }}</a></td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.senha }}</td>
                <td>{{ entity.limiteAprovacao }}</td>
                <td>{{ entity.telefone }}</td>
                <td>{{ entity.celular }}</td>
                <td>{{ entity.ativo }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('funcionario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('funcionario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('funcionario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
