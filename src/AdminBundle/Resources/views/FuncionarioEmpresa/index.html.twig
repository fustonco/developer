{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FuncionarioEmpresa list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Ativo</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('funcionarioempresa_show', { 'id': entity.id }) }}">{{ entity.ativo }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('funcionarioempresa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('funcionarioempresa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('funcionarioempresa_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
