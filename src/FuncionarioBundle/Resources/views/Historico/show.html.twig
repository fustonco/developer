{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Historico</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Codigo</th>
                <td>{{ entity.codigo }}</td>
            </tr>
            <tr>
                <th>Datapassagem</th>
                <td>{{ entity.dataPassagem|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('historico') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('historico_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
